const loadingPage = (() => {
  return {
    render: () => {
      return `<img src = "../images/loading.gif" alt = "loading!">`
    },
    listeners: () => {}
  }
})();

export default loadingPage;